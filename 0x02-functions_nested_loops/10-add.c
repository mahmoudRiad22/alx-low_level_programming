#include "main.h"
/**
 * add - is a function that adds two numbers and return the result
 *
 * @number1: the 1st number
 * @number2: the 2nd number
 *
 * Return: the sum of number1, number2.
 */
int add(int number1, int number2)
{
	return (number1 + number2);
}
